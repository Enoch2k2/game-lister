<h1>Create List for <%= @game.name %></h1>

<%= form_for([@game, @list]) do |f| %>
  <div class="input-field">
    <%= f.text_field :name %>
    <%= f.label :name %><br/>
  </div>

  <%= f.submit "Create List", class: "btn" %>
<% end %>